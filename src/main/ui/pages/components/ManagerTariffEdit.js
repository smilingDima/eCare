import DataGrid from 'react-data-grid';
import Link from 'next/link'

const ManagerTariffEdit = (props) => {
    const columns = [{
        name: 'Name', key: 'name',
    }, {
        name: 'Price', key: 'price',
    }, {
        name: 'Actions', key: 'id',
    },];
}